let b = [1, 10, 9, 100, 1000, 23, 13, 12, 2, 5]
//sort b in increasing order
// b.sort(fucntion(a, c){
//     return a - c //if a<b -> a -b will be negative -. if cmp function gives negative then a is placed before a
// }); //sort is an HOF.. the sort function takes a comparator function as argument.